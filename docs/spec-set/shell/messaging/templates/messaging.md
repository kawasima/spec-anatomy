---
name: <メッセージング契約の業務的名前>
description: <このトピック/キューが業務上伝えること>
status: draft
last-reviewed: YYYY-MM-DD
---

# MSG-XXX <メッセージング契約の業務的名前>

## このメッセージング契約の業務的役割

<このトピック/キューが業務上何を伝えるか。誰が発行し、誰が購読するか。何のために連携するか。3〜5文の散文で書く。>

## 関連する Core の data / behavior

メッセージのペイロードに対応する Core のドメインイベント、または発行する `behavior`。

- `data <ドメインイベント名>` ([../../../spec-model/data/<file>.md](../../../spec-model/data/<file>.md))
- `behavior <発行する behavior 名>` ([../../../spec-model/behavior/<file>.md](../../../spec-model/behavior/<file>.md))

## 基本仕様

| 項目                | 値                                              |
| ------------------- | ----------------------------------------------- |
| 種別                | パブサブ（トピック） / キュー / イベントストリーム |
| トピック名/キュー名 | `<topic-name>`                                  |
| 発行者              | <発行するシステム/コンポーネント>                |
| 購読者              | <購読するシステム/コンポーネント>                |
| 配信保証            | At-most-once / At-least-once / Exactly-once     |
| 順序保証            | 全体順序 / パーティション順序 / 順序保証なし    |
| パーティションキー  | <キーに使う属性。順序保証なしなら N/A>          |
| 保持期間            | <ブローカーでのメッセージ保持期間>              |
| 暗号化              | あり（方式） / なし                             |
| 認証認可            | <認証方式と必要権限>                            |
| ブローカー          | Kafka / RabbitMQ / SQS / Pub/Sub / 等           |

## ペイロード形式

| 項目                | 値                                              |
| ------------------- | ----------------------------------------------- |
| 形式                | JSON / Avro / Protobuf / その他                 |
| スキーマレジストリ  | <Schema Registry の URL または N/A>             |
| バージョニング方針  | <スキーマ進化のルール>                          |
| 文字コード          | UTF-8 / 等                                      |

## ヘッダ

メッセージブローカーのヘッダ仕様。Cloud Events や AWS EventBridge などの規約に従う場合はそれを明記。

| ヘッダ名              | 必須 | 値                  | 用途                     |
| --------------------- | ---- | ------------------- | ------------------------ |
| ce-id / messageId     | ○    | <UUID 等>           | メッセージ重複検知       |
| ce-source / source    | ○    | <発行元識別>        | 送信元識別               |
| ce-type / eventType   | ○    | <イベントタイプ>    | ルーティング判定         |
| ce-time / timestamp   | ○    | <ISO 8601>          | 発生時刻                 |

## ペイロード項目

| No | 項目名     | 項目ID | 必須 | データ型                                             | 多重度        | デフォルト値 | フォーマット仕様       | 派生元                       |
| --- | ---------- | ------ | ---- | ---------------------------------------------------- | ------------- | ------------ | ---------------------- | ---------------------------- |
| 1   | <項目名>   | <ID>   | ○ / − | string / number / boolean / object / array          | 1 / 0..1 / 1..n | <デフォルト> | <フォーマット指定>     | `<data 名>.<属性>`           |

## ペイロードサンプル

```json
{
  "messageId": "<UUID>",
  "eventType": "<イベントタイプ>",
  "occurredAt": "<ISO 8601>",
  "data": {
    "<key>": "<value>"
  }
}
```

## ドメインイベントとの対応

このメッセージが発行する/購読するドメインイベント。

- 発行する場合: Core の `data <ドメインイベント名>` ([../../../spec-model/data/<file>.md](../../../spec-model/data/<file>.md))
- 購読する場合: 別システムが発行するイベント仕様への参照

## 業務的事前条件・事後条件

### 発行側

- 事前条件: <このメッセージが発行される業務状態>
- 事後条件: <発行後に成り立つ業務状態>

### 購読側

- 事前条件: <このメッセージを受信した時点の業務状態>
- 事後条件: <処理完了後の業務状態>

## 冪等性

- 発行側の重複送信可能性: あり（At-least-once の場合）/ なし（Exactly-once の場合）
- 受信側の冪等性確保方法: <messageId による重複検知 / 補償ロジック>

## エラー処理

| エラー条件         | 処理                                  |
| ------------------ | ------------------------------------- |
| ペイロード不正     | DLQ（デッドレターキュー）へ移送       |
| 業務不変条件違反   | DLQ へ移送 + 運用通知                 |
| 一時的失敗         | リトライ <回数>回、間隔 <ms>          |

## 受け入れ基準

| ケース           | 入力                  | 期待動作                                |
| ---------------- | --------------------- | --------------------------------------- |
| 正常系           | <ペイロード例>        | <期待結果>                              |
| 重複メッセージ   | 同じ messageId        | 冪等処理により1回だけ反映               |
| ペイロード不正   | <壊れたデータ>        | DLQ へ移送                              |

実装場所: `<契約テストのパス>`

## 実装への対応

- 発行コード: `<コード側のパス>`
- 購読コード: `<コード側のパス>`
- スキーマファイル: `<.json / .avsc / .proto のパス>`
- 関連 ADR: [<ADR-XXX>](../../../adrs/<ADR-XXX>-<title>.md)

## 派生元参照のチェック

- すべてのペイロード項目に Core からの派生元が書かれているか
- Core にない項目をペイロードで発明していないか
- 配信保証と業務上の要件（重複の許容、順序の必要性）が整合しているか
