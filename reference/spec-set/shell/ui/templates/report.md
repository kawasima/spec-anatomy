---
name: <帳票の業務的名前>
description: <この帳票が業務上提供すること>
status: draft
last-reviewed: YYYY-MM-DD
---

# REPORT-XXX <帳票の業務的名前>

## この帳票の業務的役割

<この帳票が業務上何を提供するか。誰のための出力か。何を伝えるか。3〜5文の散文で書く。>

## 関連する behavior / data

帳票が表示する Core の `data`、または帳票生成のトリガとなる `behavior`。

- `data <data 名>` ([../../../spec-model/data/<file>.md](../../../spec-model/data/<file>.md))
- `behavior <behavior 名>` ([../../../spec-model/behavior/<file>.md](../../../spec-model/behavior/<file>.md))

## 帳票の物理仕様

| 項目         | 値                                          |
| ------------ | ------------------------------------------- |
| 帳票様式     | <帳票様式名>                                |
| 用紙サイズ   | A3 / A4 / A5 / B4 / B5 / その他             |
| 用紙向き     | 縦 / 横                                     |
| ページ数     | 固定 / 可変、最大ページ数                   |
| 処理方式     | オンライン即時 / バッチ                     |
| 出力タイミング | <業務イベント、または時刻>                |
| 出力契機     | <業務イベント / 定期実行 / ユーザ操作>     |

## 項目グループ

帳票は項目グループ単位で構造化します。各グループは出力条件と一覧表示の有無を持ちます。

### 項目グループ1: <項目グループ名>

- 出力条件: <このグループが出力される業務条件。例: 注文金額が10万円以上のとき>
- 2ページ目以降の出力: あり / なし
- 一覧表示（明細グループの場合のみ）:
  - 明細の単位: <例: 注文明細1行>
  - 明細の数: <最大件数。0 なら制限なし>
  - 並び順: <ソートKEY、昇順/降順>

#### 項目

| No | 項目名   | 必須 | データタイプ                | 表示最大Byte | 横揃え          | 縦揃え        | フォント                 | デフォルト値 | 編集仕様           | 派生元                              |
| --- | -------- | ---- | --------------------------- | ------------ | --------------- | ------------- | ------------------------ | ------------ | ------------------ | ----------------------------------- |
| 1   | <項目名> | ○ / − | 文字列 / 数値 / 日付 / 通貨 | 100          | 左 / 中央 / 右 | 上 / 中央 / 下 | <フォント種類, サイズ>   | <デフォルト値> | <フォーマット仕様> | `<data 名>.<属性>` または `sum(...)` |

### 項目グループ2: <項目グループ名>

<同様>

## 業務的事前条件・出力契機

- 事前条件: <この帳票が出力される業務状態（Core の `data` の状態として記述）>
- 出力契機: <業務イベント / 定期実行 / ユーザ操作>

## 影響する Core の data

- [`<data 名>`](../../../spec-model/data/<file>.md)

## 多言語対応

| 言語 | 対応 | 別ファイル                                |
| ---- | ---- | ----------------------------------------- |
| ja   | ○    | このファイル                              |
| en   | ○ / − | <別ファイルへのリンク または i18n リソース> |

## 受け入れ基準

| ケース                       | 入力データ           | 期待出力                       |
| ---------------------------- | -------------------- | ------------------------------ |
| 正常系                       | <データ例>           | <期待される帳票内容>           |
| 項目グループの出力条件不成立 | <データ例>           | 該当グループは出力されない     |

実装場所: `<帳票テスト・スナップショットテストのパス>`

## 実装への対応

- 帳票テンプレート: `<テンプレートファイルパス（Excel / HTML / PDF テンプレート）>`
- 帳票生成コード: `<コード側のパス>`
- 関連 ADR: [<ADR-XXX>](../../../adrs/<ADR-XXX>-<title>.md)

## 派生元参照のチェック

- すべての項目に Core からの派生元が書かれているか
- 集計（sum、count）が `<関数名>(<引数>)` の形式で書かれているか
- Core にない項目を帳票側で発明していないか
