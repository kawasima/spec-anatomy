---
name: <メールの業務的名前>
description: <このメールが業務上伝えること>
status: draft
last-reviewed: YYYY-MM-DD
---

# MAIL-XXX <メールの業務的名前>

## このメールの業務的役割

<このメールが業務上何を伝えるか。誰に、いつ送るか。受信者がこのメールを読んで取るべきアクションは何か。3〜5文の散文で書く。>

## 関連する behavior / data

メール送信のトリガとなる Core の `behavior`、または埋め込まれる `data`。

- `behavior <behavior 名>` ([../../../spec-model/behavior/<file>.md](../../../spec-model/behavior/<file>.md))
- `data <data 名>` ([../../../spec-model/data/<file>.md](../../../spec-model/data/<file>.md))

## メールの基本仕様

| 項目                  | 値                                          |
| --------------------- | ------------------------------------------- |
| 送信元 (From)         | <差出人メールアドレス>                       |
| 送信元 (Reply-To)     | <返信先メールアドレス。なければ From と同じ> |
| 送信先 (To)           | <受信者の業務的指定。例: 注文者の登録メールアドレス> |
| 送信先 (Cc)           | <Cc 指定。なければ「なし」>                  |
| 送信先 (Bcc)          | <Bcc 指定。監査用 Bcc があるならここに>      |
| エラー時送信先        | <配信エラー時の通知先>                       |
| 暗号化                | 必要 / 不要                                 |
| メール形式            | テキスト / HTML                             |
| 文字コード（件名）    | UTF-8 / ISO-2022-JP / 等                    |
| 文字コード（本文）    | UTF-8 / ISO-2022-JP / 等                    |
| 添付ファイル          | あり / なし                                 |
| 処理方式              | オンライン即時 / 非同期 / バッチ            |
| 送信タイミング        | <業務イベント、または時刻>                  |
| 送信元取引            | `behavior <名前>` ([../../../spec-model/behavior/<file>.md](../../../spec-model/behavior/<file>.md)) |

## 件名

```text
<件名のテンプレート文字列。{0} などの埋め込みプレースホルダを含めてよい>
```

## 本文

```text
<本文のテンプレート文字列。
{0}, {1} のような埋め込みプレースホルダを使う。
HTMLメールの場合は HTML タグを含めて書く。>
```

## 埋め込み文字列定義

本文と件名に埋め込む変数の定義。

| プレースホルダ | 項目名     | 物理名     | 表示桁数（半角） | 編集仕様           | 派生元                          |
| -------------- | ---------- | ---------- | ---------------- | ------------------ | ------------------------------- |
| {0}            | <項目名>   | <変数の物理名> | 50               | <フォーマット指定> | `<data 名>.<属性>`              |
| {1}            | <項目名>   | <変数の物理名> | 100              | <フォーマット指定> | `<data 名>.<属性>` または `<関数>(...)` |

## メールヘッダ

カスタムヘッダがある場合のみ記述。

| ヘッダ名         | 値              | 用途                       |
| ---------------- | --------------- | -------------------------- |
| X-Custom-Header  | <値>            | <用途>                     |
| X-Message-Id     | <UUID>          | メール重複検知             |

## 電子署名

電子署名がある場合のみ記述。署名方式と証明書 ID を書く。

- 署名方式: <S/MIME など>
- 証明書 ID: <証明書識別子>

## 添付ファイル定義

添付ファイルがある場合のみ記述。

| 添付ファイル概要 | 種類                        | 作成仕様                       | 派生元                        |
| ---------------- | --------------------------- | ------------------------------ | ----------------------------- |
| <概要>           | PDF / Excel / CSV / 等      | <生成方法、テンプレート参照>   | `<data 名>` または `<behavior>` |

## 多言語対応

| 言語 | 対応 | 件名・本文ファイル                     |
| ---- | ---- | -------------------------------------- |
| ja   | ○    | このファイル                           |
| en   | ○ / − | <別ファイル または i18n リソースのキー> |

顧客の登録言語設定で自動切り替えする場合は、その判断ルールを明示する。

## 業務的事前条件・送信契機

- 事前条件: <このメールが送信される業務状態（Core の `data` の状態として記述）>
- 送信契機: <業務イベント / 定期実行>

## 影響する Core の data

- [`<data 名>`](../../../spec-model/data/<file>.md)

## 受け入れ基準

| ケース           | 入力データ           | 期待される送信内容                        |
| ---------------- | -------------------- | ----------------------------------------- |
| 正常系           | <データ例>           | 件名、本文、宛先が埋め込み定義通りに展開  |
| 配信エラー       | <データ例>           | エラー時送信先への通知                    |
| 重複イベント受信 | 同じ messageId       | 1回しか送信されない（X-Message-Id で重複検知） |
| 多言語対応       | <データ例>           | 言語別のテンプレートで送信                |

実装場所: `<メール送信テストのパス>`

## 実装への対応

- メールテンプレート: `<テンプレートファイルパス>`
- メール送信コード: `<コード側のパス>`
- 関連 ADR: [<ADR-XXX>](../../../adrs/<ADR-XXX>-<title>.md)

## 派生元参照のチェック

- すべての埋め込み文字列に Core からの派生元が書かれているか
- Core にない埋め込み変数を発明していないか
- 編集仕様（フォーマット、桁数）が業務的に正しいか
