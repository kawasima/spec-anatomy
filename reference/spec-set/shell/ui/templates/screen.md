---
name: <画面の業務的名前>
description: <この画面が業務上提供すること>
status: draft
last-reviewed: YYYY-MM-DD
---

# SCREEN-XXX <画面の業務的名前>

## この画面の業務的役割

<この画面が業務上何を提供するか。誰のための画面か。どのユースケースの実現手段か。3〜5文の散文で書く。>

## 関連する behavior

この画面が呼び出す Core の `behavior`。

- `behavior <behavior 名>` ([../../../spec-model/behavior/<file>.md](../../../spec-model/behavior/<file>.md))

画面が表示する `data` も明示する。

- `data <data 名>` ([../../../spec-model/data/<file>.md](../../../spec-model/data/<file>.md))

## 想定利用ユーザ

- 主アクター: <ロール名（Core の `data` で定義したロール）>
- 副アクター: <他のロール>

## 一覧表示

画面に一覧表示領域がある場合のみ記述。なければこのセクションを削除する。

| 領域名 | ページング | ソート条件        |
| ------ | ---------- | ----------------- |
| <領域名> | あり / なし | <ソートKEY、昇順/降順> |

## レイアウトバリアント

業務概念は1つで、デバイスや利用文脈ごとに項目セットと編集仕様を分けて書く。共通する派生元（Core の `data`）は同じ。

### PC版

#### 画面項目グループ: <領域名>

No 列はレイアウトバリアント内（PC版／スマホ版／タブレット版）でグループをまたいだ連番とする。設計書の項目番号として図中の赤枠等に対応させる。バリアントが変われば連番は1から振り直す。

| No | 項目名 | 種別 | 派生元 | 編集仕様 | 必須 | 初期値 | 表示条件 |
| -- | ------ | ---- | ------ | -------- | ---- | ------ | -------- |
| 1  | <項目名> | label / text / textarea / select_pulldown / radio / checkbox / hidden / file / button / link / image | `<data 名>.<属性>` または共通コンポーネント呼び出し | <フォーマット指定や表示変換> | ○ / − | <初期値> | 常に表示 / role >= MANAGER / status = Approved 等 |

#### 画面項目グループ: <別の領域名>

| No | 項目名 | 種別 | 派生元 | 編集仕様 | 必須 | 初期値 | 表示条件 |
| -- | ------ | ---- | ------ | -------- | ---- | ------ | -------- |

### スマホ版

PC版から表示項目を絞り込む場合は、PC版から派生する形で書く。完全に別構造のときは独立してテーブルを書く。

#### 画面項目グループ: <領域名>

| No | 項目名 | 種別 | 派生元 | 編集仕様 | 必須 | 初期値 | 表示条件 |
| -- | ------ | ---- | ------ | -------- | ---- | ------ | -------- |

### タブレット版

省略可。PC版と同じならこのセクションを削除する。

## 画面イベント

| イベント名 | 発生タイミング | サーバ通信 | 対応する behavior / API | 正常時遷移先 |
| ---------- | -------------- | ---------- | ----------------------- | ------------ |
| <イベント名> | onClick ボタンA / onSubmit フォームB / onChange フィールドC | あり_同期 / あり_非同期 / なし | `behavior <名前>` または [API-XXX](../../api/...md) | [SCREEN-YYY](SCREEN-YYY.md) |

### イベント詳細

#### <イベント名>

- バリデーション: 境界での Core の型への変換ルール
- 呼び出す behavior / API: <参照>
- 遷移条件:
  - 正常終了 → [SCREEN-YYY](SCREEN-YYY.md) へ
  - 業務エラー（Core の Result の Err 枝）→ 本画面に留まりエラーメッセージ表示
  - 状態不整合エラー → 本画面に留まり「他の操作によって状態が変わりました」と表示し再読み込み
  - 認証エラー → ログイン画面へ
  - サーバエラー → エラー画面へ

## 業務的事前条件・事後条件

- 事前条件: <この画面に到達できる業務状態。Core の `data` の状態として記述>
- 事後条件: <この画面の操作完了後に成り立つ業務状態>

## 影響する集約・データ

Core の `data` で、この画面が表示・操作対象とするもの。

- [`<data 名>`](../../../spec-model/data/<file>.md)

## アクセス制御

- 認証必須: はい / いいえ
- 必要ロール: <Core で定義したロール>
- 動的な表示制御: 上の「表示条件」列で個別に制御

## 受け入れ基準

| ケース   | 操作         | 期待結果           |
| -------- | ------------ | ------------------ |
| 正常系   | <操作>       | <期待結果>         |
| 業務エラー | <操作>     | <期待結果>         |
| 状態不整合 | <操作>     | <期待結果>         |

実装場所: `<E2Eテストファイルのパス>`

## 実装への対応

- フロント側コンポーネント: <コード側のパス>
- レスポンシブ実装方針: CSS メディアクエリで吸収する範囲と JS で分岐する範囲を明示
- 関連 ADR: [<ADR-XXX>](../../../adrs/<ADR-XXX>-<title>.md)

## 派生元参照のチェック

このSpec Setの規約として、各項目には Core の `data` からの派生元を明示する。

- すべての項目に派生元が書かれているか
- Core にない項目を画面側で発明していないか
- 派生計算（sum、count、フォーマット変換）が `<関数名>(<引数>)` の形式で書かれているか

## ツール連携

この screen.md フォーマットは [tools/oppsett](../../../../../tools/oppsett/) のパーサ・ライタが依存しています。フォーマットを変更する場合は `tools/oppsett/src/screen-md-parser.js` と `screen-md-writer.js` の追従が必要です。
